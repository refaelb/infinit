name: CI

on:
  push:
    branches: [ "test" ]
jobs:
  build:
    macos:
      xcode: "9.0" # uses Xcode version
    resource_class: large
    step:
      - name: Check out the repo
        uses: actions/checkout@v3


      # name: echo 
      # run: echo "hi "
