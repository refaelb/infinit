name: CI

on:
  push:
    branches: [ "test" ]
jobs:
  build:
    docker:
      - image: cimg/base:2022.09
    step:
      - name: Check out the repo
        uses: actions/checkout@v3


      # name: echo 
      # run: echo "hi "
