name: CI

on:
  push:
    branches: [ "test" ]
jobs:
  build:
    machine: "true"
    step:
      name: Check out the repo
      run: actions/checkout@v3

     


  build:
    machine: "true"
    step:
      name: Check out the repo
      run: echo "hi "
