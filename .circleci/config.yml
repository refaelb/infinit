version: 2.1
name: Docker Image CI

on:
  push:
    branches: [ "test" ]
  pull_request:
    branches: [ "test" ]

jobs:
  build:
    machine: true
    steps:
      - name: Hello world action step
        id: hello