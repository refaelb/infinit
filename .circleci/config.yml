name: CI

on:
  push:
    branches: [ "test" ]
jobs:
  build:
    machine: "true"
    step:
      name: Check out the repo
      run: actions/checkout@v3

     



    step:
      name: Check out the repo
      run: echo "hi "
