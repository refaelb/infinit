name: CI

on:
  push:
    branches: [ "test" ]
jobs:
  build:
    runs-on: ubuntu-latest
    step:
      - name: Check out the repo
        uses: actions/checkout@v3


      # name: echo 
      # run: echo "hi "
