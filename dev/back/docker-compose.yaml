services:

  mongodb:
    container_name: mongodb
    image: mongo
    restart: always

  python_app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - MONGO_URI=mongodb
    ports:
      - "3000:3000"